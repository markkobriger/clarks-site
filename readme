# Prime Maintenance LLC Website (GitHub Pages)

Static website for Prime Maintenance LLC (Luxemburg, WI).

## Live site
After you enable GitHub Pages:
`https://YOUR-USERNAME.github.io/YOUR-REPO/`

## Files
- `index.html` - main page
- `styles.css` - site styling
- `script.js` - mobile menu + contact form SMS builder
- `assets/` - images (logo, hero, gallery, favicon)

## Replace the placeholder images
Put your real images here (keep names the same to avoid editing code):
- `assets/logo.png`
- `assets/hero.jpg`
- `assets/gallery-1.jpg`
- `assets/gallery-2.jpg`
- `assets/gallery-3.jpg`
- `assets/favicon.png`

Tip: download photos from the business Facebook page and drop them into `/assets`.

## Local preview
Just open `index.html` in a browser.
(Optional) If you want a local server:
- VS Code extension: Live Server

## Deploy on GitHub Pages
1. Create a repo (example: `prime-maintenance-site`)
2. Upload the files to the `main` branch
3. GitHub repo Settings -> Pages
4. Source: Deploy from a branch
5. Branch: `main` and folder: `/root`
6. Save. Wait for the Pages URL to appear.

## Want a real contact form?
GitHub Pages has no backend. If you want form submissions, we can connect:
- Formspree
- Netlify Forms
- A simple Google Form endpoint
